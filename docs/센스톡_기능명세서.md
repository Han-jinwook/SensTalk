# [센스톡(SenseTalk)] 올인원 통합 기능 명세서

> **문서 버전:** v5.0 (다중 블록 순차 발송 엔진, 전 채널 옴니 클립보드 주입, 8대 영역 휴먼 지터 보안 방어, 복합 컬럼 동적 치환 및 전수 기능 명세)  
> **최종 갱신:** 2026-09-20  
> **핵심 가치:** 엑셀 복붙 지옥 탈출, 수동 조작/우회로 전면 배제, 유저 파일 맞춤 동적 치환, [Enter] 타건만으로 끝나는 안전한 1:1 고객 관리 옴니채널 자동화 솔루션

---

# PART 1. 서비스 비전 및 인터랙션 메커니즘

## 1. 단일 브랜드 통합: "센스톡(SenseTalk)" 올인원
* **명칭 일원화:** 센스봇(SenseBot)이라는 별도 브랜딩을 사용자 대면 영역에서 완전히 배제하고, 오직 **"센스톡(SenseTalk)"** 단일 브랜드로 일원화.
* **올인원 솔루션:** 웹 프론트엔드 작업 캔버스와 로컬 가속 엔진(`sensebot.py`)이 결합된 단일 패키지.
* **수동 우회로 전면 폐지:**
  * 어중간한 '수동 복사로 진행' 등 사용자에게 혼란을 주는 분기 옵션을 전면 소거.
  * 오직 **① 엔진 실행 ➔ ② 메신저 대화창 열기 ➔ ③ [Enter] 타건 연속 전진**의 100% 자동화 파이프라인에 올인.

---

## 2. 채널 선택 ➔ 단일 시작 버튼 ➔ [Enter] 순차 발송 워크플로우

```
┌────────────────────────────────────────────────────────────────────────┐
│                   센스톡 4단계 표준 옴니 발송 프로세스                  │
└────────────────────────────────────────────────────────────────────────┘

 [1단계: 데이터 및 메시지 준비]
  - 엑셀/CSV 파일 드롭 또는 붙여넣기로 보유 명단 로드 (임의 복합 컬럼 무제한 지원)
  - 텍스트 블록 및 사진(이미지) 블록 조립, 실시간 개인화 치환 결과 확인

 [2단계: 글로벌 6대 옴니채널 선택]
  - [💬 카카오톡] / [✈️ 텔레그램] / [🟢 라인] / [📱 문자(SMS)] / [💬 왓츠앱] / [🟢 위챗]
  - 채널 탭 선택 즉시 로컬 엔진(`sensebot.py`)에 활성 채널 실시간 동기화

 [3단계: 발송 시작 버튼 1회 클릭]
  - 엔진 연결 시: 발송 루프 가동 ➔ 메인 버튼이 `[⏸️ 발송 일시정지 [F9키]]`로 자동 토글
  - 엔진 미연결 시: 3단계 가이드 모달 자동 팝업 (원클릭 엔진 다운로드 제공)
  - 명단 전체 발송 완료 시: 오발송 방지 안전 락(Safety Lock) 자동 발동 (비활성화)

 [4단계: 다중 블록 순차 발송 ➔ 유저의 유일한 행동은 오직 [Enter] 탕! 탕!]
  - [블록 1: 텍스트] 
    * 텔레그램/라인/왓츠앱/SMS: 개인화 본문이 채워진 딥링크로 대화창 즉시 오픈
    * 카카오톡/위챗: 1:1 대화창 자동 포커스 및 텍스트 자동 주입
    👉 유저 조작: 화면 보고 [Enter] 1회 타건 (텍스트 전송 확정)
  
  - [블록 2: 사진(이미지) - 0.4~1.0초 무작위 보안 지터 후 자동 장전]
    * 엔진이 이미지 데이터를 Windows 클립보드(CF_DIB)에 자동 장전
    * 활성화된 메신저 창에 [Ctrl + V] 자동 주입 ➔ 메신저 자체 사진 전송 모달 팝업
    👉 유저 조작: 화면 보고 [Enter] 1회 타건 (사진 전송 확정)
  
  - [수신자 완료 처리 및 다음 대상 전진]
    * 모든 블록 전송 확정 시에만 잔여 크레딧 1건 정밀 차감
    * 카카오톡의 경우 창 닫기(ESC), 다음 고객 대화방 자동 검색 및 장전!
```

---

## 3. PC 카카오톡 2대 포커스 특성 활용 (마우스 0회 달성)

1. **단축키 [Ctrl + F]:** 마우스로 검색창을 조준할 필요 없이, 카톡 메인창 어디서든 친구 검색창에 텍스트 커서 즉시 진입.
2. **1:1 대화방 오픈 시:** 친구 검색 후 [Enter]를 쳐서 대화창이 열리는 순간, 대화방 맨 아래의 메시지 입력란(`RICHEDIT50W`)에 커서 100% 자동 진입.

이 두 가지 특성 덕분에 **마우스 클릭 0회, 창 전환 0회**의 완벽한 자동 장전이 완성됩니다.

---

# PART 2. 동적 수신자 명단 및 실시간 변수 치환 엔진

## 1. 명단 관리 및 영구 동기화
* **하드코딩 목업 전면 폐지:** 정적인 예시 목업 데이터를 완전히 들어내고, 사용자가 실제 입력/수정한 최신 명단만을 항상 유지.
* **로컬 스토리지 동기화 (`sensetalk_saved_recipients`):** 명단 업로드, 행 추가, 삭제, 수정 발생 즉시 브라우저 로컬 저장소에 영구 보관되어 새로고침 후에도 데이터 100% 유지.

## 2. 유저 파일 맞춤 동적 복합 컬럼(헤더) 파싱 규칙
* **임의 복합 컬럼 무제한 수용:** `이름`, `회사`, `직책`, `전화번호`, `메모`뿐만 아니라 `고객등급`, `계약일`, `만기일`, `포인트`, `환급액`, `예약시간` 등 유저가 업로드한 엑셀/CSV의 모든 컬럼명을 동적으로 인식하여 수신자 객체의 속성으로 매핑.
* **비문자열 데이터 타입(숫자/날짜) 무손실 보간:** 정수/실수/날짜형 데이터(예: `25000` ➔ `25000P`)를 유실 없이 문자열로 안전 형변환하여 치환.
* **컬럼명이 없는 파일 업로드 시 자동 유추 폴백:**
  - 1번째 컬럼 ➔ `이름`
  - 2번째 컬럼 ➔ `항목1` (전화번호 형식 감지 시 `전화번호`)
  - 3번째 컬럼 ➔ `항목2`
  - N번째 컬럼 ➔ `항목N`

## 3. 실시간 텍스트 변수 치환 엔진 (`getFullMessageForRecipient` / `get_blocks_for_recipient`)
* **치환자 문법:** `#{컬럼명}` (예: `#{이름}`, `#{고객등급}`, `#{포인트}`)
* **치환 규칙:**
  1. 수신자 데이터 객체에 해당 키가 존재하면 해당 값으로 즉시 치환.
  2. 일치하는 컬럼이 없는 경우 `#{컬럼명}` 원본 문자열을 깨뜨리지 않고 안전하게 보존.
  3. 광고 블록(`isAd: true`) 설정 시 `(광고)` 접두사 및 `무료수신거부: {optOutNum}` 번호 자동 부착.

## 4. 글로벌 연락처 자동 정규화 엔진 (`get_recipient_phone`)
* **다변화된 연락처 헤더 통합 파싱:** `phone`, `전화번호`, `연락처`, `휴대폰`, `핸드폰`, `mobile`, `tel` 중 존재하는 필드를 자동 탐색하여 추출.
* **국제전화 번호 자동 변환 (WhatsApp 전용):** `010`, `011`, `016` 등 `01`로 시작하는 국내 모바일 번호의 맨 앞 `0`을 제거하고 국가코드 `82`를 붙여 `8210...` 형식으로 100% 자동 변환.

---

# PART 3. 크레딧 차감 및 정산 라이프사이클

## 1. 과금 원칙: "수신자 1인당 최종 발송 완료 시 1회만 차감" (Zero False / Zero Over Billing)
* **테스트 클릭 차감 원천 방지:** UI 상에서 캔버스를 수정하거나, 수신자를 클릭해 미리보기를 보거나, 단순 복사 테스트를 수행할 때는 **크레딧이 절대 차감되지 않음**.
* **다중 블록 통합 단일 차감:**
  - 한 명의 고객에게 텍스트 1개, 사진 1개 등 총 2개 이상의 블록을 순차 전송하더라도, **최종 블록 전송이 성공하여 수신자가 `done` 상태로 전환될 때 단 1크레딧만 정산**.
* **정밀 차감 트리거:**
  - 로컬 PC 가속 엔진이 메신저에 모든 블록 발송을 성공시키고, `sent_and_advancing` 이벤트를 프론트엔드로 콜백 전송했을 때만 1건씩 정산(`handleCreditDeduction()`).

## 2. 요금제 및 쿼터 체계
* **무료 체험 (Free):** 최초 **100건 무료 체험** (카드 등록/로그인 없이 즉시 발송 가능).
* **스타터 (Starter):** **월 3,000원** / 월 **1,000건** 한도 (건당 3.0원).
* **프로 (Pro - 주력 추천):** **월 6,000원** / 월 **3,000건** 한도 (건당 2.0원).
* **비즈니스 (Business):** **월 12,000원** / 월 **9,000건** 한도 (건당 1.33원).

---

# PART 4. PC 가속 엔진 통신 규격 및 옴니 순차 발송 엔진

## 1. 로컬 HTTP REST 통신 규격 (`http://127.0.0.1:28888`)

| 엔드포인트 | Method | 역할 및 페이로드 스펙 |
| :--- | :--- | :--- |
| `/health` | `GET` | 엔진 데몬 가동 여부 및 일일 발송 통계 확인 (`{ status: "ok", today_sent: N, daily_limit: 500 }`) |
| `/sync` | `POST` | 웹 캔버스 상태(수신자 목록, 블록 데이터, 활성 채널 `channel`, 현재 인덱스)를 엔진 큐에 동기화 |
| `/start` | `POST` | 키보드 Enter 훅 활성화 및 자동 연속 발송 루프 기동 |
| `/pause` | `POST` | 발송 일시정지 (키보드 `[F9]` 키 또는 화면 버튼 연동) |
| `/poll` | `GET` | 실시간 이벤트 폴링 (`last_event`: `loaded_waiting_enter`, `sent_and_advancing`, `all_completed`, `paused`) |
| `/dispatch` | `POST` | 하위 호환용 단일 메시지 즉시 장전 |

## 2. 다중 블록(Multi-Block) 순차 발송 엔진 메커니즘
1. **블록 시퀀스 구성 (`get_blocks_for_recipient`):**
   - 수신자별로 치환이 완료된 유효 텍스트 블록과 base64 이미지 데이터를 원본 순서 그대로 배열 리스트로 빌드.
2. **동일 대화방 내 순차 전송 루프:**
   - **블록 0 장전:** 대화방 오픈 및 첫 번째 블록(텍스트) 장전 ➔ `WAITING_FOR_USER_ENTER = True`.
   - **1차 [Enter] 타건 감지:** 대화방을 닫지 않고 유지 ➔ 다음 블록 유무 확인.
   - **블록 1(사진) 자동 주입:** `0.4~1.0초` 휴먼 지터 후 Windows 클립보드(`CF_DIB`) 장전 ➔ `Ctrl + V` 타건 ➔ 사진 전송 모달 안착 ➔ `WAITING_FOR_USER_ENTER = True`.
   - **2차 [Enter] 타건 감지:** 최종 사진 전송 확정 ➔ 수신자 `done` 갱신 ➔ `sent_and_advancing` 발행.
   - **창 닫기 및 전진:** 카카오톡의 경우 자동 오픈 대화창 ESC 닫기 ➔ 다음 대기 고객 자동 로드.

## 3. Windows 클립보드 비트맵(`CF_DIB`) 자동 주입 메커니즘
* **표준 비트맵 변환 파이프라인:**
  - Base64 DataURL (PNG/JPEG) ➔ Pillow(PIL) 디코딩 ➔ 24-bit RGB BMP 변환 ➔ 14바이트 `BITMAPFILEHEADER` 제거 ➔ 순수 `CF_DIB` 바이너리 스트림 추출.
  - Windows 클립보드에 `CF_DIB` 포맷으로 안전 등록 (`win32clipboard` 또는 Win32 API `SetClipboardData`).
* **마우스 드래그 0% 전 채널 네이티브 호환:**
  - 텔레그램, 라인, 왓츠앱, 위챗, SMS(휴대폰과 연결), 카카오톡 모두 Windows 클립보드의 `CF_DIB` 비트맵을 `Ctrl + V` 단축키로 수신하여 자체 사진 미리보기/전송 화면을 즉시 띄움.

## 4. 휴먼 지터(Human-like Jitter) 8대 영역 무작위 난수화 보안 엔진
기계적 매크로 고정 딜레이(Fixed Milliseconds)를 전면 폐지하고 사람 특유의 가변적 물리 반응 시간을 모사하여 안티봇/휴리스틱 탐지 원천 차단:

| 영역 | 동작 내용 | 적용된 휴먼 지터 난수 범위 | 보안 방어 목적 |
| :---: | :--- | :---: | :--- |
| **1** | **키보드 단일 타건 (`press_key`)** | `0.025초 ~ 0.065초` (25~65ms) | 손가락 누름 압력 및 릴리즈 시간의 물리적 불규칙성 재현 |
| **2** | **단축키 조합 (`press_hotkey`)** | `Ctrl` 누름 `20~50ms`, `V` 누름 `25~65ms` | 복합 단축키 타건 시 인간 손가락의 미세 시차 재현 |
| **3** | **블록 간 전송 텀 (텍스트 ➔ 사진)** | **`0.4초 ~ 1.0초`** 완전 난수 | 이전 말풍선 전송 확인 후 다음 사진을 올리는 인간 반응 시차 |
| **4** | **수신자 완료 후 다음 대상 전진 텀** | **`0.35초 ~ 0.75초`** 완전 난수 | 한 사람 완료 후 다음 대상을 부르는 자연스러운 호흡 리듬 |
| **5** | **마우스 클릭 & 포커스 안착** | 클릭 다운/업 `15~35ms`, 포커스 대기 `40~80ms` | 마우스 버튼 물리적 누름 및 윈도우 포커스 반응 지터 |
| **6** | **메시지 붙여넣기 렌더링 대기** | `0.14초 ~ 0.24초` 난수 | 메신저 텍스트 프레임워크의 입력 감지 대기 시차 분산 |
| **7** | **창 전환 및 포그라운드 활성화** | `0.09초 ~ 0.16초` 난수 | Windows OS 데스크톱 핸들 스위칭 반응 시차 가변화 |
| **8** | **친구 검색 및 1:1 대화방 오픈** | 검색 `80~150ms`, 대화창 팝업 `420~650ms` | 검색창 활성화 및 대화방 팝업 대기시간 불규칙화 |

## 5. 지능형 창 닫기 정책 (Smart ESC)
* 대량 발송 시 봇이 자동 오픈한 중간 수신자 창만 ESC로 깔끔히 닫아 바탕화면 창 범람 방지.
* 유저가 수동으로 미리 열어둔 대화창(`_pre_opened: true`)이거나 마지막 대상일 때는 전송 후 대화창을 닫지 않고 유지.

---

# PART 5. 글로벌 6대 옴니채널 발송 메커니즘 규격

| 채널 | 프로토콜 / 기술 메커니즘 | 사진 전송 메커니즘 (`CF_DIB`) | 안티밴 안전 캡 |
| :--- | :--- | :--- | :--- |
| **💬 카카오톡** | 최신 PC(v26.8) Win32 제어 + `Ctrl+F` 친구 검색 + 텍스트 주입 + Enter | `CF_DIB` 주입 ➔ Ctrl+V ➔ [클립보드 이미지 전송] 팝업 안착 ➔ Enter | 500건/일 |
| **✈️ 텔레그램** | 공식 `tg://msg?text=...` OS 딥링크 직통 호출 | `CF_DIB` 주입 ➔ Ctrl+V ➔ **텔레그램 사진 전송 모달 즉시 팝업** ➔ Enter | 500건/일 |
| **🟢 라인(LINE)** | `https://line.me/R/msg/text/?...` 딥링크 자동 채움 | `CF_DIB` 주입 ➔ Ctrl+V ➔ **라인 대화창 이미지 자동 안착** ➔ Enter | 500건/일 |
| **💬 왓츠앱** | `https://wa.me/{국제번호}?text=...` 다이렉트 딥링크 | `CF_DIB` 주입 ➔ Ctrl+V ➔ **왓츠앱 사진 전송 미리보기 오버레이** ➔ Enter | 300건/일 |
| **📱 문자(SMS)** | `sms:{전화번호}?body=...` Windows 휴대폰과 연결 연동 | `CF_DIB` 주입 ➔ Ctrl+V ➔ **휴대폰과 연결 MMS 사진 자동 첨부** ➔ Enter | 500건/일 |
| **🟢 위챗(WeChat)** | 위챗 PC 창 자동 포커스 + 클립보드 안전 복사 주입 | `CF_DIB` 주입 ➔ Ctrl+V ➔ **위챗 입력창 사진 썸네일 자동 기입** ➔ Enter | 150건/일 (보안특화) |

## 1. 포그라운드 활성 창 정밀 검증 (`is_valid_messenger_window`)
* `psutil` 라이브러리를 통해 현재 유저가 보고 있는 포그라운드 윈도우의 프로세스명 확인:
  - 카카오톡: `is_chat_window` (`EVA_Window` 클래스, 대화창 규격 검사) or 이미지 팝업 모달 (`300~500px`)
  - 텔레그램: `telegram.exe`
  - 라인: `line.exe`
  - 위챗: `wechat.exe`, `wechatapp.exe`
  - 왓츠앱: `whatsapp.exe` 또는 브라우저 창(`chrome.exe`, `whale.exe` 등) 제목에 "WhatsApp" 포함 여부 검증
  - 문자(SMS): `phoneexperiencehost.exe`, `yourphone.exe`
* 타사 프로그램(메모장, 탐색기 등)에서 발생한 Enter 타건은 철저히 무시하여 오발송 원천 차단.

---

# PART 6. 안전 및 예외 처리 정책

1. **Human-in-the-loop 원칙:**
   - 최종 전송 결정은 사용자가 대화창을 확인하고 물리적 [Enter]를 누름으로써 이루어지므로, 메신저사의 무차별 백그라운드 봇 탐지 알고리즘을 원천 회피.
2. **명단 완료 시 자동 발송 차단 정책 (All-Done Safety Lock):**
   - 명단 전체의 발송 상태가 `done`인 경우 시작 버튼이 비활성화되며 발송이 차단됨.
   - 상단 [🔄 상태 초기화] 버튼을 명시적으로 눌러야만 `pending`으로 리셋되어 재발송 가능.
3. **비상 정지 (`[F9]`):**
   - 발송 도중 예기치 못한 상황(오타, 오발송 위험 등) 발생 시 즉시 키보드 `[F9]`를 누르면 0.01초 내로 엔진 루프 정지.
4. **F8 전역 단축키 수동 제어:**
   - 윈도우 전역 핫키 `[F8]`을 등록하여 마우스 조작 없이도 발송 시작 및 강제 다음 대상 전진 가능.
5. **안티밴 일일 한도 강제 (Daily Limit Guard):**
   - 로컬 파일(`bot_stats.json`)에 일자별 발송량을 누적 기록하여, 1일 최대 500건 초과 시 하드 리밋에 의해 발송 자동 중단.
